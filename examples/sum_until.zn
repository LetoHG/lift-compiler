func sum_until(upper : i32, threshold : i32) -> i32 {
    let sum : i32 = 0;
    for i in 0..100 {
        sum += i;
        if sum > threshold {
            return i;
        }
    }
    return 0;
}

return fun();

func main() {
    unknown1(a, c + 3);
    return sum_until(20, 27);
}
